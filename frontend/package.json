{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "npm run clean && tsc && cp src/index.html src/index.css dist/",
    "clean": "rm -Rf dist",
    "serve": "npm run build && http-server -p 8081 dist/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.3.3"
  }
}
